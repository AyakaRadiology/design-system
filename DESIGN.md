---
name: "@ayaka/design-system — biomonitor"
description: "Generated house tokens for needle instrument reviews"
colors:
  "bg": "oklch(0.2 0.015 225)"
  "bg-subtle": "oklch(0.16 0.015 225)"
  "bg-muted": "oklch(0.25 0.015 225)"
  "bg-elevated": "oklch(0.29 0.015 225)"
  "border": "oklch(0.3 0.012 225)"
  "border-strong": "oklch(0.42 0.012 225)"
  "text": "oklch(0.94 0.008 100)"
  "text-secondary": "oklch(0.72 0.008 100)"
  "text-tertiary": "oklch(0.55 0.008 100)"
  "accent": "oklch(0.8 0.12 178)"
  "accent-hover": "oklch(0.86 0.11 178)"
  "accent-fg": "oklch(0.22 0.03 200)"
  "accent-subtle": "oklch(0.32 0.05 183)"
  "success": "oklch(0.75 0.14 155)"
  "success-subtle": "oklch(0.31 0.05 155)"
  "success-fg": "oklch(0.22 0.05 155)"
  "warning": "oklch(0.79 0.13 85)"
  "warning-subtle": "oklch(0.31 0.05 85)"
  "warning-fg": "oklch(0.23 0.05 85)"
  "danger": "oklch(0.72 0.16 25)"
  "danger-hover": "oklch(0.78 0.16 25)"
  "danger-subtle": "oklch(0.31 0.06 25)"
  "danger-fg": "oklch(0.22 0.06 25)"
  "info": "oklch(0.74 0.12 245)"
  "info-subtle": "oklch(0.31 0.05 245)"
  "info-fg": "oklch(0.22 0.05 245)"
  "chart-1": "oklch(0.66 0.12 178)"
  "chart-2": "oklch(0.53 0.16 252)"
  "chart-3": "oklch(0.67 0.15 308)"
  "chart-4": "oklch(0.55 0.17 345)"
  "chart-5": "oklch(0.62 0.15 105)"
  "x-glass-fill": "oklch(0.29 0.015 225 / 0.96)"
  "x-glass-fill-strong": "oklch(0.29 0.015 225 / 0.98)"
  "x-glass-border": "oklch(0 0 0 / 0.4)"
  "x-glass-highlight": "oklch(0.94 0.008 100 / 0.18)"
typography:
  text-xs:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "0.75rem"
    lineHeight: "1rem"
  text-sm:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "0.875rem"
    lineHeight: "1.125rem"
  text-base:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "1rem"
    lineHeight: "1.375rem"
  text-lg:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "1.25rem"
    lineHeight: "1.75rem"
  text-xl:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "1.5rem"
    lineHeight: "2rem"
  text-2xl:
    fontFamily: "\"Inter Variable\", \"Inter\", ui-sans-serif, system-ui, -apple-system, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif"
    fontSize: "1.75rem"
    lineHeight: "2.25rem"
rounded:
  "md": "0.375rem"
  "lg": "0.5rem"
  "glass": "8px"
spacing:
  "0.5": "calc(0.25rem * 0.5)"
  "1": "calc(0.25rem * 1)"
  "1.5": "calc(0.25rem * 1.5)"
  "2": "calc(0.25rem * 2)"
  "3": "calc(0.25rem * 3)"
  "4": "calc(0.25rem * 4)"
  "6": "calc(0.25rem * 6)"
  "8": "calc(0.25rem * 8)"
  "10": "calc(0.25rem * 10)"
  "12": "calc(0.25rem * 12)"
---

<!-- GENERATED by tools/generate-design.ts. Run bun run design:generate; do not edit. -->
# Design System: @ayaka/design-system

## Overview

This projection selects biomonitor for the needle instrument context. The package also ships base (light/dark); this does not change its defaults.
Sources: [schema](tokens/schema.json), [voice](voices/biomonitor.css), [scales](tokens/scales.css), [UI rules](docs/ui-rules.md), and the lockfile-pinned Tailwind theme.
Review within [PRODUCT.md](PRODUCT.md), [AGENTS.md](AGENTS.md), and these sources. House roles and material rules outrank generic detector suggestions.
Change sources, then regenerate. DESIGN.md is a projection, never an input to token generation.

## Colors

| Token | Value | Role / purpose |
| --- | --- | --- |
| `--bg` | `oklch(0.2 0.015 225)` (:root) | surface: Surfaces that sit above the page: cards, panels, inputs, toolbars. |
| `--bg-subtle` | `oklch(0.16 0.015 225)` (:root) | surface: Page chrome, table headers, alternating rows — one step behind bg. |
| `--bg-muted` | `oklch(0.25 0.015 225)` (:root) | surface: Inset wells and disabled fills — one step in front of bg. |
| `--bg-elevated` | `oklch(0.29 0.015 225)` (:root) | surface: Popovers, menus, dialogs, tooltips — one step above bg; the depth cue where a voice forbids shadows. |
| `--dialog-surface` | `var(--bg-elevated)` (@layer components .ds-dialog-content) | surface: Element-scoped DialogContent surface and DialogBody fade stop. DialogContent defaults it to --bg-elevated; a consumer may override it in the theme file when re-toning one dialog. |
| `--border` | `oklch(0.3 0.012 225)` (:root) | border: Structural borders and dividers. |
| `--border-strong` | `oklch(0.42 0.012 225)` (:root) | border: Control borders and hover borders — the visible edge of an input or a secondary button. |
| `--text` | `oklch(0.94 0.008 100)` (:root) | text: Body text and headings. Clears AA on every surface. |
| `--text-secondary` | `oklch(0.72 0.008 100)` (:root) | text: Labels, units, meta. Clears AA on bg. |
| `--text-tertiary` | `oklch(0.55 0.008 100)` (:root) | text: Placeholders and decorative meta. Deliberately below AA; never load-bearing. |
| `--accent` | `oklch(0.8 0.12 178)` (:root) | accent: The one hue that brands the product: primary fills, links, the focus ring. |
| `--accent-hover` | `oklch(0.86 0.11 178)` (:root) | accent: Hover state of an accent fill. |
| `--accent-fg` | `oklch(0.22 0.03 200)` (:root) | accent: Text, icons and knobs drawn ON an accent fill. |
| `--accent-subtle` | `oklch(0.32 0.05 183)` (:root) | accent: Tinted chips and selected rows — a wash of the accent that body text still reads on. |
| `--success` | `oklch(0.75 0.14 155)` (:root) | status: Healthy state. |
| `--success-subtle` | `oklch(0.31 0.05 155)` (:root) | status: Tinted background for a success badge or row. |
| `--success-fg` | `oklch(0.22 0.05 155)` (:root) | status: Text and icons drawn ON a solid success fill. |
| `--warning` | `oklch(0.79 0.13 85)` (:root) | status: Degraded state, needs attention but not action. |
| `--warning-subtle` | `oklch(0.31 0.05 85)` (:root) | status: Tinted background for a warning badge or row. |
| `--warning-fg` | `oklch(0.23 0.05 85)` (:root) | status: Text and icons drawn ON a solid warning fill. |
| `--danger` | `oklch(0.72 0.16 25)` (:root) | status: Failed state, and the only status hue that may be used as text. |
| `--danger-hover` | `oklch(0.78 0.16 25)` (:root) | status: Hover state of a danger fill. Danger is the only status used as a button fill, hence the only status with a hover. |
| `--danger-subtle` | `oklch(0.31 0.06 25)` (:root) | status: Tinted background for a danger badge or row. |
| `--danger-fg` | `oklch(0.22 0.06 25)` (:root) | status: Text and icons drawn ON a solid danger fill. |
| `--info` | `oklch(0.74 0.12 245)` (:root) | status: Neutral informational state. |
| `--info-subtle` | `oklch(0.31 0.05 245)` (:root) | status: Tinted background for an info badge or row. |
| `--info-fg` | `oklch(0.22 0.05 245)` (:root) | status: Text and icons drawn ON a solid info fill. |
| `--chart-1` | `oklch(0.66 0.12 178)` (:root) | series: Categorical chart series slot 1. Series hues are data and status hues are alarms — a series that reads as a status tells the viewer something the data never said, which is what the ΔE floor in src/lint/voices.test.ts holds apart. |
| `--chart-2` | `oklch(0.53 0.16 252)` (:root) | series: Categorical chart series slot 2. Series hues are data and status hues are alarms — a series that reads as a status tells the viewer something the data never said, which is what the ΔE floor in src/lint/voices.test.ts holds apart. |
| `--chart-3` | `oklch(0.67 0.15 308)` (:root) | series: Categorical chart series slot 3. Series hues are data and status hues are alarms — a series that reads as a status tells the viewer something the data never said, which is what the ΔE floor in src/lint/voices.test.ts holds apart. |
| `--chart-4` | `oklch(0.55 0.17 345)` (:root) | series: Categorical chart series slot 4. Series hues are data and status hues are alarms — a series that reads as a status tells the viewer something the data never said, which is what the ΔE floor in src/lint/voices.test.ts holds apart. |
| `--chart-5` | `oklch(0.62 0.15 105)` (:root) | series: Categorical chart series slot 5. Series hues are data and status hues are alarms — a series that reads as a status tells the viewer something the data never said, which is what the ΔE floor in src/lint/voices.test.ts holds apart. |
| `--x-glass-fill` | `oklch(0.29 0.015 225 / 0.96)` (:root) | surface: Translucent floating surface fill; AA body and secondary text over black and white backdrops. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-fill-strong` | `oklch(0.29 0.015 225 / 0.98)` (:root) | surface: Denser floating surface fill for busy imagery; same AA contract. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-border` | `oklch(0 0 0 / 0.4)` (:root) | border: Bottom-right shaded edge of the shared 1px glass gradient border. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-highlight` | `oklch(0.94 0.008 100 / 0.18)` (:root) | border: Top-left light edge of the shared glass gradient border. Schema-owned despite the historical x- prefix; not a consumer extension. |

## Typography

| Token | Value | Role / purpose |
| --- | --- | --- |
| `--font-sans` | `"Inter Variable", "Inter", ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` (@theme) | font: Prose: labels, copy, headings. |
| `--font-mono` | `"JetBrains Mono Variable", "JetBrains Mono", ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace` (@theme) | font: Every reading a machine produced: numerals, counters, eyebrows. Pair it with tabular-nums. |
| `--text-xs` | `0.75rem` (@theme) | type: Type step xs (12/16 — meta, column headers, hints). |
| `--text-xs--line-height` | `1rem` (@theme) | type: Line height paired with text-xs. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--text-sm` | `0.875rem` (@theme) | type: Type step sm (14/18 — body base for tool UI). |
| `--text-sm--line-height` | `1.125rem` (@theme) | type: Line height paired with text-sm. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--text-base` | `1rem` (@theme) | type: Type step base (16/22 — body base for reading surfaces). |
| `--text-base--line-height` | `1.375rem` (@theme) | type: Line height paired with text-base. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--text-lg` | `1.25rem` (@theme) | type: Type step lg (20/28 — section headings). |
| `--text-lg--line-height` | `1.75rem` (@theme) | type: Line height paired with text-lg. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--text-xl` | `1.5rem` (@theme) | type: Type step xl (24/32 — page titles). |
| `--text-xl--line-height` | `2rem` (@theme) | type: Line height paired with text-xl. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--text-2xl` | `1.75rem` (@theme) | type: Type step 2xl (28/36 — rare display use). |
| `--text-2xl--line-height` | `2.25rem` (@theme) | type: Line height paired with text-2xl. Tailwind reads the `--name--line-height` suffix as the pair. |
| `--container-prose-page` | `45rem` (@theme) | type: Reading-surface line length; generates max-w-prose-page. Distinct from Tailwind's built-in max-w-prose. |
| `--numeric-empty-scale` | `0.25` (:root) | type: How quiet a missing reading is: the placeholder's size as a fraction of the readout's own, floored at the xs step. |

## Layout

- Spacing (padding/gap/margin): Carbon steps only — `0.5, 1, 1.5, 2, 3, 4, 6, 8, 10, 12` (2–48px). Never 5/7/9/11. Width/max-width/container utilities are exempt (`w-96`, `max-w-3xl` fine), plus `w-px`/`h-px` hairlines; control heights come from the Density table, not this exemption.

| Token | Value | Role / purpose |
| --- | --- | --- |
| spacing-0.5 | `calc(0.25rem * 0.5)` | House spacing step; Tailwind spacing unit |
| spacing-1 | `calc(0.25rem * 1)` | House spacing step; Tailwind spacing unit |
| spacing-1.5 | `calc(0.25rem * 1.5)` | House spacing step; Tailwind spacing unit |
| spacing-2 | `calc(0.25rem * 2)` | House spacing step; Tailwind spacing unit |
| spacing-3 | `calc(0.25rem * 3)` | House spacing step; Tailwind spacing unit |
| spacing-4 | `calc(0.25rem * 4)` | House spacing step; Tailwind spacing unit |
| spacing-6 | `calc(0.25rem * 6)` | House spacing step; Tailwind spacing unit |
| spacing-8 | `calc(0.25rem * 8)` | House spacing step; Tailwind spacing unit |
| spacing-10 | `calc(0.25rem * 10)` | House spacing step; Tailwind spacing unit |
| spacing-12 | `calc(0.25rem * 12)` | House spacing step; Tailwind spacing unit |

## Elevation & Depth

| Token | Value | Role / purpose |
| --- | --- | --- |
| `--trace-glow` | `color-mix(in oklab, var(--accent) 45%, transparent)` (:root) | signature: The colour of the ONE glow a voice is allowed, on its signature element only. Optional: a voice with no signature element declares no glow. Derive it from --accent rather than restating a hue, so retuning the accent cannot leave the glow behind. |
| `--z-raised` | `10` (:root) | z: A sticky header or a raised row, above the page and nothing else. |
| `--z-overlay` | `100` (:root) | z: Dropdowns, popovers, select menus, and a dialog's scrim. |
| `--z-modal` | `1000` (:root) | z: Dialog content, above its own overlay. |
| `--z-toast` | `1100` (:root) | z: Toasts and tooltips — the layer that must clear a modal. |
| `--motion-fast` | `120ms` (:root) | motion: State changes the eye should not have to wait for: hover, press, a switch knob. |
| `--motion-base` | `200ms` (:root) | motion: Entrances and exits: a dialog, a popover, a toast. |
| `--ease-standard` | `cubic-bezier(0.2, 0, 0, 1)` (@theme) | motion: The one easing curve. Fast out of the gate, settled at the end. |

### Floating Glass

Use the shared [Glass material](docs/glass.md), including its opaque/reduced-transparency behavior. These tokens are schema-owned despite their historical x- prefix; they are not consumer extensions.

| Token | Value | Role / purpose |
| --- | --- | --- |
| `--x-glass-fill` | `oklch(0.29 0.015 225 / 0.96)` (:root) | surface: Translucent floating surface fill; AA body and secondary text over black and white backdrops. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-fill-strong` | `oklch(0.29 0.015 225 / 0.98)` (:root) | surface: Denser floating surface fill for busy imagery; same AA contract. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-border` | `oklch(0 0 0 / 0.4)` (:root) | border: Bottom-right shaded edge of the shared 1px glass gradient border. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-highlight` | `oklch(0.94 0.008 100 / 0.18)` (:root) | border: Top-left light edge of the shared glass gradient border. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-shadow` | `inset 0 1px 2px oklch(0 0 0 / 0.06), 0 8px 24px oklch(0 0 0 / 0.32)` (:root) | surface: Shared subtle inner shadow and soft outer elevation shadow. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-blur` | `10px` (:root) | surface: Backdrop blur in px, at most 12px for the Surface ARM Windows GPU budget. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-saturate` | `1.08` (:root) | surface: Slight backdrop saturation multiplier. Schema-owned despite the historical x- prefix; not a consumer extension. |
| `--x-glass-radius` | `8px` (:root) | surface: Single corner radius shared by floating surfaces. Schema-owned despite the historical x- prefix; not a consumer extension. |

## Shapes

- Radii: `rounded-md` controls, `rounded-lg` surfaces, `rounded-full` pills/dots. Nothing else.

| Token | Value | Role / purpose |
| --- | --- | --- |
| md | `0.375rem` | House Tailwind radius |
| lg | `0.5rem` | House Tailwind radius |
| glass | `8px` | Shared floating surface |

## Components

Use the exported [React primitives and their contracts](README.md#primitives). Domain components belong in consumer repositories; do not replace shared controls with local lookalikes.

## Do's and Don'ts

The active voice's rules below are copied from voices/biomonitor/VOICE.md on every generation. The shared density and scale rules remain binding: [docs/ui-rules.md](docs/ui-rules.md).

### Voice: biomonitor

Patient-telemetry aesthetics for dashboards and monitors the owner glances at.
First worn by spine (PRs #11–12); `../biomonitor.css` carries the gate-verified
values (AA pairs ≥ 4.5, including glass and actual StatusPill pairs; chart-vs-status ΔE ≥ 10).

#### When to reach for it

A personal or small-team monitoring surface — system health, agents, money,
telemetry — where the page's job is "is everything alive, and did anything need
me?" and the owner should *enjoy* looking at it. Not for content/reading
surfaces or marketing.

#### The rules that make it read as an instrument

1. **Dark only, blue-cast, layered.** No light mode, no toggle. Surfaces step
   ~0.04–0.05 L apart (page → card → well); the steps ARE the depth — no
   `shadow-*` on ordinary surfaces. Floating panels may use the shared
   `Glass` material described in `docs/glass.md`; its edge gradient and subtle
   elevation shadow are the sole material exception. Never pure black: hue 225 at low chroma everywhere.
2. **Warm-white text on cold surfaces** (hue ~100 vs ~225) — the temperature
   contrast is half the look.
3. **Numerals are mono, and that's a constant, not a habit.** One shared
   constant (spine: `NUMERIC = "font-mono tabular-nums"`) applied to every
   figure, cost, age, counter. Micro-labels/eyebrows: mono, `text-xs`,
   `uppercase tracking-widest`, secondary color. Prose stays in the sans.
   Container-level mono over short label phrases is fine — it reads as an
   instrument caption, not a bug.
4. **One glow, on the signature element only** (`--trace-glow`, accent-derived).
   Ordinary surfaces are flat and bordered; floating Glass uses neutral elevation,
   never a second accent glow. A second glow demotes the first.
5. **Status hues are alarms, series hues are data** — never cross them, and the
   signature draws in the accent phosphor regardless of state: you read the
   *rhythm*, not the color (a color-coded signature is just a third status dot).
   Status labels never blink or pulse in any state; use the shared mapping
   in `docs/ui-rules.md` and `StatusPill`'s enforced static treatment. Dialog
   scrollbars use secondary text against the elevated surface; the functional
   bottom fade is the sole scroll-cue exception to the no-gradients rule.
6. **Pick the signature from the subject's own world.** spine is a nervous
   system, so its signature is a live EKG in the header: steady rhythm when
   healthy, idle grey until data arrives, a flatline exactly when the staleness
   alarm fires. Derive yours the same way — from what the product *is*, driven
   by real state, with `prefers-reduced-motion` honored (static, never absent).
7. **Mechanize the look's invariants.** Contrast floors, series/status ΔE
   separation, and a dark-only guard that rejects any second theme block. This
   package does all three for every voice in `src/lint/voices.test.ts`, ported
   from spine's `frontend/src/lib/contrast.{ts,test.ts}`, so a voice added here
   inherits them rather than re-deriving them.

#### What this voice is not

Not a hacker terminal (pure black + single acid accent), no decorative glassmorphism or gradients beyond the shared floating Glass material. The base ui skill's scale/density rules all still hold — this voice
only re-values the tokens and adds the rules above.
